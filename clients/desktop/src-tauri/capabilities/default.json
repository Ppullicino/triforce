{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimum desktop permissions for window state and external links",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "window-state:default",
    "updater:default"
  ]
}
